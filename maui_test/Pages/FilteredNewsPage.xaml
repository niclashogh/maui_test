<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="maui_test.Pages.FilteredNewsPage"
             xmlns:converts="clr-namespace:maui_test.Pages.Converters"
             Title="Filtered News">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converts:UnixToDateConverter x:Key="UnixToDateConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ScrollView>

        <CollectionView ItemsSource="{Binding Stories}">

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout Padding="10">
                        <Label Text="{Binding Title}" FontSize="18" FontAttributes="Bold"/>
                        <Label Text="{Binding Time, Converter={StaticResource UnixToDateConverter}}" FontSize="14"/>
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>

    </ScrollView>
    
</ContentPage>